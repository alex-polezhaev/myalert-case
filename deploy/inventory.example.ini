# Note: use a dedicated unprivileged deploy user with sudo (not root).
# Replace the placeholder hosts with your real ones (or keep as env-specific).

[my-alert-frontend]
main ansible_host=frontend.example.com ansible_user=deploy

[my-alert-backend]
main ansible_host=backend.example.com ansible_user=deploy
